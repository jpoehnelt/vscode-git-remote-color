---
"vscode-git-remote-color": minor
---

Always color title bar and activity bar. Removed `affectTitleBar` and `affectActivityBar` settings — these elements are now always colored. Use `gitRemoteColor.elementAdjustments` to lighten/darken individual elements.
